Problem statement:
Write a Python program that takes an integer as input and checks whether it is an Armstrong number.

Solution:
num = int(input())
temp = num
digits = len(str(num))
total = 0
while temp > 0:
    digit = temp % 10
    total += digit ** digits
    temp //= 10
if total == num:
    print("Armstrong")
else:
    print("Not Armstrong")
